<!DOCTYPE html>
<html lang="en">

<head>
    <title>Examination</title>
    <link rel="stylesheet" href="../CSS/Main.css">
    <link rel="stylesheet" href="../CSS/Exam.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container">
            <ol class="list-group list-group my-3 bg-dark">
                <li class="list-group-item list-group-item-warning">Subject Name: <%- examname %></li>
                <li class="list-group-item list-group-item-warning">Questions: 10 questions</li>
                <li class="list-group-item list-group-item-warning">Time: 60 Minutes</li>
            </ol>
              
            <form id="exam" method="post" action="/final">
                <input type="hidden" name="courseId" value="<%=courseid%>">
                <input type="hidden" name="courseName" value="<%=examname%>">
                <input type="hidden" name="examid" value="<%=examid%>">
                <input type="hidden" name="stundentInfo" value="<%=studinfo%>">
                <% for(var i=0; i < exam.length; i++) { %>
                    <div class="card">
                        <h5 class="card-header bg-dark text-white">Question <%- i+1 %> </h5>
                        <div class="card-body">
                            <p>
                                <%= i+1  %>. <%= exam[i].question %>
                            </p>
                            <% for(var j=0; j < exam[i].queChoices.length; j++) { %>
                                <input type="radio" id="<%= exam[i].queChoices[j].choiceDescription%>" name="<%= exam[i].question %>" value="<%= exam[i].queChoices[j].choiceChar%>" class = "form-check-input" required>
                                <label class="form-check-label" for="<%= exam[i].queChoices[j].choiceDescription%>"><%= exam[i].queChoices[j].choiceChar%> ) <%= exam[i].queChoices[j].choiceDescription%></label><br>
                                <% } %>
                            </div>
                        </div>
                                    <br>
                                    <% } %>
                        

                    
                    
                                <div class="card p-3">
                                    <input type="submit" class="btn btn-primary bg-gradient col-6 mx-auto">
                                </div>
                                
            </form>

    </div>
</body>

</html>